---
import Badge from '../ui/Badge.astro';
import Breadcrumb from '../ui/Breadcrumb.astro';
import type { ScientificMetadata } from '../../types/content';
import { withBase } from '../../lib/url';
import type { Locale } from '../../lib/i18nUrl';
import { useTranslations } from '../../i18n';

export interface Props {
  title: string;
  tagline?: string;
  topicTitle: string;
  topicSlug: string;
  difficulty: 'beginner' | 'intermediate' | 'advanced';
  estimatedTime: '5min' | '10min' | '15min' | '20min' | '30plus';
  scientific?: ScientificMetadata;
  isFallback?: boolean;
  locale?: Locale;
  class?: string;
}

const {
  title,
  tagline,
  topicTitle,
  topicSlug,
  difficulty,
  estimatedTime,
  scientific,
  isFallback = false,
  locale = 'en',
  class: className = '',
} = Astro.props;

const isId = locale === 'id';
const t = useTranslations(locale);

const breadcrumbItems = [
  { label: isId ? 'Jelajahi' : 'Explore', href: isId ? '/id/explore' : '/explore' },
  { label: topicTitle, href: isId ? `/id/explore/topics/${topicSlug}` : `/explore/topics/${topicSlug}` },
  { label: title, current: true },
];
---

<header class={`border-b border-[var(--color-border)] pb-8 mb-8 ${className}`}>
  <Breadcrumb items={breadcrumbItems} class="mb-4" />

  <div class="space-y-4">
    <div class="flex items-center gap-2 flex-wrap text-xs">
      <a
        href={withBase(isId ? `/id/explore/topics/${topicSlug}` : `/explore/topics/${topicSlug}`)}
        class="font-semibold uppercase tracking-wider text-[var(--color-accent)] hover:underline"
      >
        {topicTitle}
      </a>
      <span class="text-[var(--color-border)]">•</span>
      <Badge variant={difficulty}>{t.cards.difficulty[difficulty] || difficulty}</Badge>
      <Badge variant="time">{t.cards.time[estimatedTime] || estimatedTime}</Badge>

      {isFallback && (
        <span class="px-2 py-0.5 rounded text-[10px] font-mono font-bold uppercase bg-amber-100 dark:bg-amber-950 text-amber-900 dark:text-amber-200 border border-amber-300 dark:border-amber-800" title={t.common.untranslatedBadgeLabel}>
          {t.common.untranslatedBadge}
        </span>
      )}

      {scientific && (
        <span
          class="px-2.5 py-0.5 rounded-full text-xs font-mono font-medium bg-[var(--color-surface-hover)] border border-[var(--color-border)] text-[var(--color-text-muted)] inline-flex items-center gap-1"
          title={`Source: ${scientific.source.join(', ')} • Confidence: ${scientific.confidence}`}
        >
          <svg class="w-3 h-3 text-[var(--color-secondary)]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
          </svg>
          <span class="capitalize">{scientific.sourceType.replace('-', ' ')}</span>
        </span>
      )}
    </div>

    <h1 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold text-[var(--color-text)] tracking-tight leading-tight">
      {title}
    </h1>

    {tagline && (
      <p class="text-base sm:text-lg text-[var(--color-text-muted)] max-w-3xl leading-relaxed">
        {tagline}
      </p>
    )}
  </div>
</header>
